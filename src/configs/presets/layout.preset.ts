export const gamePreset = {
  perspective: 0.1,
};

export const landingPreset = {
  finger: {
    ratio: 61 / 320,
  },
  leftArrow: {
    ratio: 114 / 320,
    offsetY: -20,
  },
  rightArrow: {
    ratio: 114 / 320,
    offsetY: -20,
  },
};

export const firepowerPreset = {
  perspective: 12,
  offsetY: -10,
  ratio: 23 / 320,
};
